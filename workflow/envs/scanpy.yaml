name: scanpy_pipeline
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.14
  - scanpy
  - matplotlib  # Handles the core plotting canvas and PDF export
  - seaborn     # Handles the elegant styling and distributions
  - pandas
  - reportlab   # Handles assembling your multi-page PDF story